[pytest]
# Hard per-test timeout so a single runaway test (e.g. an unmocked AWS client
# hanging on a real socket connect, or a pagination loop fed a MagicMock whose
# LastEvaluatedKey is always truthy) is killed individually with a traceback
# instead of hanging or OOM-killing the whole suite.
timeout = 60
timeout_method = signal
